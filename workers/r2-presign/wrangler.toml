name = "r2-presign"
main = "src/index.ts"
compatibility_date = "2026-05-06"
compatibility_flags = ["nodejs_compat"]

# Public URL: https://r2-presign.<subdomain>.workers.dev (no custom domain yet).
workers_dev = true
preview_urls = true

# Secrets (set via `wrangler secret put`):
# R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_PUBLIC_BASE_URL, FIREBASE_PROJECT_IDS
